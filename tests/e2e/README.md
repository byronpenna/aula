# E2E (Playwright)

Pendiente de instalar Playwright (`npm init playwright@latest` desde este directorio)
como parte de una fase posterior de endurecimiento; por ahora se deja un spec de
referencia (`vertical-slice.spec.ts`) que documenta el flujo objetivo de la sección
19, punto 12, sin ejecutarse todavía en CI. No afirmar que corre hasta que se
configure el runner real (`npx playwright install` + config).
